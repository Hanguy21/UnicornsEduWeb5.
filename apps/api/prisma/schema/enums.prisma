// ============================================
// ENUMS
// ============================================

enum UserRole {
  admin
  staff
  student
  guest
}

enum UserStatus {
  active
  inactive
  pending
}

enum StaffStatus {
  active
  inactive
}

enum StudentStatus {
  active
  inactive
}

enum Gender {
  male
  female
}

enum ClassStatus {
  running
  ended
}

enum ClassType {
  vip
  basic
  advance
  hardcore
}

enum AttendanceStatus {
  present
  excused
  absent
}

enum WalletTransactionType {
  topup
  loan
  repayment
  extend
}

enum LessonTaskStatus {
  pending
  in_progress
  completed
  cancelled
}

enum LessonTaskPriority {
  low
  medium
  high
}

enum PaymentStatus {
  paid
  pending
}
