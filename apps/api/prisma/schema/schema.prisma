generator client {
  provider        = "prisma-client"
  output          = "../../generated/"
  moduleFormat    = "cjs"
  previewFeatures = ["fullTextSearchPostgres"]
}

datasource db {
  provider = "postgresql"
}
